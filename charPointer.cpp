//#include<bits/stdc++.h>
#include <iostream>
#include <cstring>
#include <cmath>

using namespace std;
int main()
{
  long long x = 16 * pow(2, 30) / (pow(2, 10) * 4);
  long long a = pow(2, 22);
  cout << x << " " << a << endl;

  // long long xx = pow(2, 10) * 4;
  // cout << xx << endl;
  char name[50];

  /*name[0]='j';
    name[1]='a';
    name[2]='z';
    name[3]='i';

  for(char i = 'a';i<='z';i++){
    name[i] = i;
    cout<<name[i]<<" ";
    len = strlen(name);
  }*/

  /*char c[50] = "jazi";
  cout << c << endl;
  int len = strlen(c);
  cout << "\nLength = " << len << endl;
*/
  return 0;
}
